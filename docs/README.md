# docs/

`demo.png` will be added here.

To generate it yourself, run:

```bash
uv run remit compare --amount 1000 --from GBP --to CNY
```

Screenshot the terminal output and save as `docs/demo.png`.
